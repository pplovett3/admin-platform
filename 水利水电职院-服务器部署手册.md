# 水利水电职业技术学院 - 服务器部署手册

> 适用对象：YF-Platform 课件平台 + 后续在同一台服务器上部署的其他项目
> 维护方式：本文档跟随 `admin-platform` 仓库一起维护，部署流程变更时同步更新
> 创建时间：2026-05

---

## 一、服务器基本信息

### 1.1 物理/虚拟资源

| 项目 | 值 |
|---|---|
| 宿主机 | 学校 ESXi03（`172.17.136.112`） |
| VM 名称 | `YF-Platform` |
| 操作系统 | Ubuntu 24.10（**EOL，使用 old-releases 源**，后续建议迁 24.04 LTS） |
| CPU | 8 核 |
| 内存 | 32 GB |
| 系统盘 | `/dev/sda` 200 GB（`/` 195 GB） |
| 数据盘 | `/dev/sdb` 800 GB（挂载到 `/data`） |
| IP（静态） | `172.17.136.200/24` |
| 网关 | `172.17.136.1` |
| DNS | `192.168.103.87`、`192.168.101.87` |

### 1.2 SSH 访问

```bash
ssh yfadmin@172.17.136.200
# 密码: 88888888
```

> ⚠️ **生产环境务必：**
> 1. 改默认密码（`passwd`）
> 2. 配置 SSH 公钥免密登录
> 3. 禁用 SSH 密码登录（`/etc/ssh/sshd_config`）

### 1.3 关键密码 / 凭据（请妥善保管）

| 服务 | 用户名 | 密码 |
|---|---|---|
| Linux 系统 | `yfadmin` | `88888888` |
| YF 平台超管（首次登录） | `Admin` | `admin123`（手机号：13800000000） |
| MinIO 控制台 | `yfadmin` | `yfPlatform@2026` |
| Portainer | 首次访问自行设置 | — |

---

## 二、目录结构（重要！）

所有数据都落在 800 G 数据盘 `/data` 上，**系统盘只放系统**。

```
/data
├── docker/                    # Docker 自身的镜像/层数据（compose 之外的所有镜像）
├── yf-platform/               # YF 平台代码 + compose
│   ├── docker-compose.yml
│   ├── .env                   # 所有密钥（不要 git 提交！）
│   ├── server/                # 后端源码（解压自 server-src.tar.gz）
│   ├── web/                   # 前端源码（解压自 web-src.tar.gz）
│   ├── server-src.tar.gz      # 上次上传的源码包
│   └── web-src.tar.gz
├── infra/                     # 基础设施 compose（每个一个目录）
│   ├── portainer/docker-compose.yml
│   └── minio/docker-compose.yml
└── storage/                   # 业务数据（持久化卷）
    ├── files/                 # YF 后端用户上传文件（容器内 /storage）
    ├── mongo/                 # MongoDB 数据
    └── minio/                 # MinIO 对象存储数据
```

**铁律**：任何要持久化的容器卷一律挂到 `/data/storage/<服务名>/`，绝不用 docker named volume（避免数据落到默认路径不知道在哪）。

---

## 三、当前已部署的服务

| 服务 | 容器名 | 端口 | 用途 | Compose 位置 |
|---|---|---|---|---|
| MongoDB | `yf-mongo` | 内部 27017 | YF 平台数据库 | `/data/yf-platform/` |
| YF 后端 | `yf-server` | `4000` | Node.js + Express + TS | `/data/yf-platform/` |
| YF 前端 | `yf-web` | `3001` | Next.js 14 SSR | `/data/yf-platform/` |
| Portainer | `portainer` | `9000` / `9443` | Web 管理 Docker | `/data/infra/portainer/` |
| MinIO | `minio` | `9100` (S3) / `9101` (UI) | 对象存储（自建 S3） | `/data/infra/minio/` |

---

## 四、首次部署完整流程（参考 / 灾备重建）

> 这是用来**从零重建服务器**的步骤。日常更新看第五章。

### 4.1 创建虚拟机（在 ESXi 控制台）

1. 登录 https://172.17.136.112 → 创建/注册虚拟机
2. 类型：典型；客户机 OS：Linux → Ubuntu 64 位
3. 资源：CPU 8、内存 32 GB
4. 硬盘：系统盘 200 GB + 数据盘 800 GB（**手动新建第二块硬盘，不要合并**）
5. 挂载 ISO：`/vmfs/volumes/.../ISO/ubuntu-24.10-live-server-amd64.iso`
6. **关键提示**：在 ESXi 网页 UI 输入数字时，每填完一个值要按 `Tab` 或 `Enter` 让 AngularJS 同步到底层数据，否则"准备完成"页可能显示成默认值。

### 4.2 安装 Ubuntu

启动 VM 后按提示走，关键选项：

- 网络：手动配置 IPv4 → 地址 `172.17.136.200/24`、网关 `172.17.136.1`、DNS `192.168.103.87,192.168.101.87`
- 磁盘：**只用 `/dev/sda`**（自定义分区/或默认 LVM 都行），`/dev/sdb` 不要动
- 用户：`yfadmin` / `88888888`
- 必勾：**Install OpenSSH server**

### 4.3 服务器初始化（一键脚本）

把下面这个脚本保存为 `init-server.sh`，从本地 SCP 上传后执行：

```bash
#!/bin/bash
# YF-Platform server bootstrap (Ubuntu 24.10 EOL workaround)
set -e

echo "=== 1/6 切换 APT 源到 old-releases（24.10 已 EOL）==="
ls /etc/apt/sources.list.d/ubuntu.sources.bak.* 2>/dev/null | xargs -r rm -f
cat > /etc/apt/sources.list.d/ubuntu.sources <<'EOF'
Types: deb
URIs: http://old-releases.ubuntu.com/ubuntu/
Suites: oracular oracular-updates oracular-backports oracular-security
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
EOF
DEBIAN_FRONTEND=noninteractive apt-get update -y

echo "=== 2/6 装常用工具 ==="
DEBIAN_FRONTEND=noninteractive apt-get install -y \
  vim git curl wget htop unzip net-tools ca-certificates gnupg lsb-release jq

echo "=== 3/6 时区 ==="
timedatectl set-timezone Asia/Shanghai

echo "=== 4/6 挂 800G 数据盘到 /data ==="
mkdir -p /data
if ! blkid /dev/sdb >/dev/null 2>&1; then
  mkfs.ext4 -L data /dev/sdb -F
fi
UUID=$(blkid -s UUID -o value /dev/sdb)
grep -q "$UUID" /etc/fstab || echo "UUID=$UUID  /data  ext4  defaults  0  2" >> /etc/fstab
mountpoint -q /data || mount /data
mkdir -p /data/docker

echo "=== 5/6 装 Docker（清华源，oracular EOL 后 Aliyun 没有这通道）==="
rm -f /etc/apt/sources.list.d/docker.list /etc/apt/keyrings/docker.asc
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
chmod a+r /etc/apt/keyrings/docker.asc
echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc] https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/ubuntu oracular stable" \
  > /etc/apt/sources.list.d/docker.list
apt-get update -y
DEBIAN_FRONTEND=noninteractive apt-get install -y \
  docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

echo "=== 6/6 配 Docker daemon（镜像加速 + data-root 落数据盘）==="
mkdir -p /etc/docker /data/docker /data/yf-platform /data/storage/{files,mongo,minio}
chown -R yfadmin:yfadmin /data/yf-platform /data/storage
cat > /etc/docker/daemon.json <<'JSON'
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live",
    "https://hub.rat.dev"
  ],
  "log-driver": "json-file",
  "log-opts": { "max-size": "100m", "max-file": "3" },
  "data-root": "/data/docker"
}
JSON
systemctl daemon-reload && systemctl restart docker
usermod -aG docker yfadmin
echo "Done. Verify:"
docker --version && docker compose version
df -hT | grep -E "/$|/data"
```

执行：
```bash
sudo bash init-server.sh
```

### 4.4 部署 Portainer + MinIO（基础设施）

```bash
# Portainer
mkdir -p /data/infra/portainer && cd /data/infra/portainer
cat > docker-compose.yml <<'YAML'
services:
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: unless-stopped
    ports: ["9000:9000", "9443:9443"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
volumes:
  portainer_data:
YAML
docker compose up -d

# MinIO
mkdir -p /data/infra/minio && cd /data/infra/minio
cat > docker-compose.yml <<'YAML'
services:
  minio:
    image: minio/minio:latest
    container_name: minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: yfadmin
      MINIO_ROOT_PASSWORD: yfPlatform@2026
    ports: ["9100:9000", "9101:9001"]
    volumes:
      - /data/storage/minio:/data
YAML
docker compose up -d
```

### 4.5 部署 YF 平台

参考第五章"日常更新流程"，把代码和 compose 推上去 build 即可。

---

## 五、日常更新流程（重点！）

> **核心思路**：本地写代码 → 打包 → SFTP 上传 → 远端 build → 重启容器。
> 所有自动化脚本都在 `admin-platform/.tools/` 里。

### 5.1 一图流

```
┌──────────────┐  tar     ┌──────────────────┐  SFTP    ┌────────────────┐
│ 本地 Windows │ ───────▶│ deploy-pkg/*.gz  │ ───────▶│ 服务器 /data/  │
│   workspace  │           │ docker-compose   │           │  yf-platform/  │
└──────────────┘           │ .env             │           └────────┬───────┘
                           └──────────────────┘                    │
                                                                   ▼
                                                            docker compose build
                                                            docker compose up -d
```

### 5.2 改了后端代码后

```powershell
# 在 d:\Admin_Platform_Project\admin-platform 目录下执行
# 1. 重新打包后端源码
tar -czf .tools\deploy-pkg\server-src.tar.gz `
  --exclude=server/node_modules --exclude=server/dist --exclude=server/run-server.log `
  server

# 2. 上传
python .tools\sftp_upload.py .tools\deploy-pkg\server-src.tar.gz /data/yf-platform

# 3. 远端解压 + build + 重启（脚本是 .tools/rebuild_server.sh）
python .tools\ssh_upload_run.py .tools\rebuild_server.sh /tmp/rebuild_server.sh
```

整个过程通常 **30 秒以内**（TS 编译很快）。

### 5.3 改了前端代码后

```powershell
tar -czf .tools\deploy-pkg\web-src.tar.gz `
  --exclude=web/node_modules --exclude=web/.next `
  --exclude=web/tsconfig.tsbuildinfo --exclude=web/dev-web.log `
  web

python .tools\sftp_upload.py .tools\deploy-pkg\web-src.tar.gz /data/yf-platform

# 远端：解压 web/ → 重 build web → 重启
python .tools\ssh_run.py "cd /data/yf-platform && rm -rf web && tar -xzf web-src.tar.gz && docker compose build web && docker compose up -d web && docker compose ps web" --sudo
```

前端 build 比较慢（**3-5 分钟**），是因为 Next.js 要 `npm run build` 一次。

### 5.4 只改环境变量（最快）

修改 `.tools/deploy-pkg/.env` 后：

```powershell
python .tools\sftp_upload.py .tools\deploy-pkg\.env /data/yf-platform
python .tools\ssh_run.py "cd /data/yf-platform && docker compose up -d --no-deps server" --sudo
```

只重启不重 build，**几秒钟**完成。常用场景：换豆包接入点 ID、改 JWT secret、改 AI key。

### 5.5 改了 docker-compose.yml 后

```powershell
python .tools\sftp_upload.py .tools\deploy-pkg\docker-compose.yml /data/yf-platform
python .tools\ssh_run.py "cd /data/yf-platform && docker compose up -d" --sudo
```

compose 会自动判断哪些容器需要重建。

---

## 六、运维常用命令

下面命令都在 **服务器上** 以 `yfadmin` 用户执行（已加入 docker 组）。

### 6.1 看状态

```bash
cd /data/yf-platform
docker compose ps                      # 看本平台所有容器
docker ps                              # 看全部容器（含 portainer / minio）
docker stats --no-stream               # 看 CPU / 内存
df -hT /data /                         # 磁盘
```

### 6.2 看日志

```bash
docker compose logs -f server          # 实时跟随
docker compose logs --tail=100 web     # 最近 100 行
docker compose logs --since=10m server # 最近 10 分钟

# 按关键字过滤
docker compose logs server | grep -iE "豆包|error"
```

### 6.3 重启 / 停止

```bash
docker compose restart server          # 软重启（不重建）
docker compose up -d --force-recreate  # 强制重建容器（不重 build 镜像）
docker compose down                    # 停止并删除容器（数据保留）
docker compose stop                    # 仅停止
docker compose start                   # 启动
```

### 6.4 进容器排错

```bash
docker compose exec server sh          # 进后端 shell
docker compose exec mongo mongosh      # 进 mongo shell
docker compose exec server printenv    # 看环境变量
```

### 6.5 备份 / 恢复

```bash
# 备份 MongoDB
docker compose exec -T mongo mongodump --archive --db admin_platform \
  > /data/backup/mongo-$(date +%F).archive

# 恢复
docker compose exec -T mongo mongorestore --archive --drop \
  < /data/backup/mongo-2026-05-27.archive

# 备份用户上传文件
tar -czf /data/backup/files-$(date +%F).tar.gz -C /data/storage files/
```

> 建议加一个 cron 每天凌晨 3 点跑：
> ```cron
> 0 3 * * * /data/scripts/backup.sh >> /data/backup/cron.log 2>&1
> ```

### 6.6 镜像清理（磁盘紧张时）

```bash
docker image prune -a -f               # 删除未使用的镜像
docker builder prune -a -f             # 清理 build 缓存
docker system df                       # 看 docker 占了多少空间
```

---

## 七、新项目部署模板

如果要在这台服务器上加部署一个新项目（比如别的演示系统），按这个模板做：

### 7.1 目录约定

```bash
sudo mkdir -p /data/<项目名>          # 项目代码 + compose
sudo mkdir -p /data/storage/<项目名>  # 项目数据卷
sudo chown -R yfadmin:yfadmin /data/<项目名> /data/storage/<项目名>
```

### 7.2 端口规划（避免冲突）

| 已占用端口 | 服务 |
|---|---|
| 22 | SSH |
| 80, 443 | （保留给将来 nginx 反代） |
| 3001 | YF web |
| 4000 | YF server |
| 9000, 9443 | Portainer |
| 9100, 9101 | MinIO |
| 27017 | MongoDB（仅容器内部） |

**新项目尽量从 80xx / 30xx / 50xx 段挑空闲端口**。

### 7.3 docker-compose.yml 模板

```yaml
services:
  app:
    image: <镜像> # 或 build: ./app
    container_name: <项目名>-app
    restart: unless-stopped
    environment:
      - KEY=${KEY}      # 引用 .env 里的变量
    volumes:
      - /data/storage/<项目名>/app-data:/app/data
    ports:
      - "<外部端口>:<内部端口>"
    networks: [<项目名>-net]

networks:
  <项目名>-net:
    driver: bridge
```

### 7.4 npm 镜像（中国网络必备）

如果是 Node 项目，Dockerfile 里 `npm install` 之前加：

```dockerfile
RUN npm config set registry https://registry.npmmirror.com
```

否则在内网会装不动。

### 7.5 Python 项目

```dockerfile
RUN pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 八、故障排查

### 8.1 容器起不来

```bash
docker compose logs <服务名>           # 看启动日志找原因
docker compose config                  # 看 compose 渲染后的最终配置
docker inspect <容器名>                # 看容器详情（环境变量、挂载等）
```

### 8.2 镜像 pull 不下来

`/etc/docker/daemon.json` 里已配了三个镜像加速器（daocloud、1panel、rat.dev）。如果还是慢/失败，临时把镜像 tag 改成对应加速器路径：

```yaml
image: docker.m.daocloud.io/library/mongo:6
```

### 8.3 apt update 报 404

Ubuntu 24.10 EOL 后只能用 `old-releases.ubuntu.com`。**不要尝试切回 archive.ubuntu.com 或 mirrors.aliyun.com**，它们都不再有 oracular 包。

### 8.4 磁盘满了

```bash
df -hT                                          # 看哪个分区满
du -sh /data/* | sort -h                        # /data 下哪个目录大
docker system df                                # docker 占用
docker image prune -a -f                        # 清未用镜像
docker logs --tail=100 <容器> | wc -l           # 单容器日志（已配 100M 上限）
```

### 8.5 内存不够

YF 平台前端 build 时会吃 ~2 GB，加上 Mongo + 平时业务，32 GB 够用。如果发现 OOM：

```bash
free -h
docker stats --no-stream | sort -k 4 -h        # 找占内存最多的容器
```

### 8.6 完全无法 SSH 到服务器

通过 ESXi Web 控制台直接打开 VM 控制台登录排查（一般是 IP 配置出问题或防火墙）。

---

## 九、未做但建议尽快做的事

### 9.1 ⚠️ 安全

- [ ] 改 `yfadmin` 默认密码
- [ ] 配 SSH 公钥免密登录，禁用密码登录
- [ ] 把 `.env` 里的 JWT_SECRET、各家 AI 的 API Key 列入 KeePass / 1Password
- [ ] 启用 ufw 防火墙（仅放行 22/3001/4000/9000/9443/9100/9101）

### 9.2 系统

- [ ] **从 Ubuntu 24.10 (EOL) 迁到 24.04 LTS**：建议方法是再开一台 VM 装 LTS，把 `/data` 整个挂过去，docker compose up 即可（数据全在 `/data` 里，零迁移成本）
- [ ] 配 cron 每日数据备份
- [ ] 装 `unattended-upgrades` 自动安全补丁

### 9.3 网络

- [ ] 申请一个学校内网域名（比如 `yf.shsldy.edu.cn`），配 nginx 反代 + 域名 SSL 证书
- [ ] 把 `.env` 里 `PUBLIC_BASE_URL` 改成新域名

### 9.4 监控

- [ ] 装 `cAdvisor` + `Prometheus` + `Grafana`（Portainer 自带的容器监控也够用了）
- [ ] 配置容器异常重启告警（钉钉 / 微信机器人）

---

## 十、本地开发机常用脚本（在 `admin-platform/.tools/` 下）

| 文件 | 用途 |
|---|---|
| `ssh_run.py` | 在远端执行单条命令（支持 `--sudo`） |
| `ssh_upload_run.py` | 上传一个 .sh 脚本到远端并执行 |
| `sftp_upload.py` | SFTP 多文件上传 |
| `init_server.sh` | 服务器初始化（4.3 那个脚本的本地副本） |
| `extract_and_infra.sh` | 解压源码 + 部署 Portainer/MinIO |
| `rebuild_server.sh` | 替换 server 源码 + build + 重启（最常用） |
| `deploy-pkg/` | 待上传文件的临时目录（`docker-compose.yml`、`.env`、`*-src.tar.gz`） |

> 这些脚本里硬编码了 IP `172.17.136.200` 和密码 `88888888`，**仅供本台服务器使用**。如果要部署到第二台，请复制一份并改 HOST / PASS。

---

## 十一、变更历史

| 日期 | 变更 | 备注 |
|---|---|---|
| 2026-05-26 | 初始部署 | Ubuntu 24.10 + Docker + YF 平台 + Portainer + MinIO |
| 2026-05-26 | 豆包接入点更换 | 旧 `ep-m-20251216144152-z7xk2` → 新 `ep-20260526174626-xrj4q` (Doubao-Seed-2.0-lite) |
| 2026-05-26 | AI 服务统一 | DeepSeek（生成课程/考题）迁移到豆包 2.0，所有 AI 调用统一一个账号 |
| 2026-05-27 | 修复分享链接 | `PUBLIC_BASE_URL` 环境变量，解决发布课程链接显示 `http://server:3001/` 的问题 |
| 2026-05-29 | 功能升级（4 项） | FBX 自动转 GLB、批量导入用户、存储配额、资源文件夹+ZIP 解压。详见第十二章。前后端镜像均已重建上线 |
| 2026-06-03 | 资源管理与存储策略明确 | 超管账号不限容量；仅超管上传的资源为公共资源（全员可见、可建三维课件）；其他用户资源为个人私有、5GB 限额；公共资源不占用个人空间。详见 12.5。前后端均需重建上线 |
| 2026-06-03 | 模型封面自动截图 | 模型首次上传时浏览器端自动生成截图作为封面并存服务器，可手动更换；资源页新增卡片视图；列表接口返回 `thumbnailUrl`，新增免登录封面接口 `GET /api/files/:id/cover`。详见 12.6。前后端均已重建上线 |
| 2026-07-21 | AI 文生图上线（豆包 Seedream 5.0） | 新增 `POST /api/ai/generate-image`：基于课程上下文由豆包 LLM 生成提示词 → 调用豆包 Seedream 5.0 文生图，用于 AI 课件配图。新增前端 `AIProcessingModal` 处理动效。新增环境变量 `DOUBAO_THINKING`、`DOUBAO_IMAGE_MODEL_ID`（已写入 `.env` 与 `docker-compose.yml`）。`DOUBAO_MODEL_ID` 保持现有接入点 `ep-20260526174626-xrj4q` 不变。前后端均已重建上线。详见 12.7 |

---

## 十二、2026-05-29 功能更新与升级说明

本次一次性上线 4 项功能，**前端 + 后端镜像均已重新构建并重启**（流程同第五章）。

### 12.1 FBX/OBJ/STL 模型上传自动转 GLB（修复 Unity 下载报错）

- **问题**：网页三维编辑器能用 three.js 直接加载 FBX，但 Unity 元宇宙大厅只能解析 glTF/GLB，
  下载以 FBX 为模型的 AI 课件时报错 `invalid glb content: bad magic (not 'glTF'): start='Kaydara FBX Binary'`。
- **修复**：在**前端上传环节**把 `.fbx/.obj/.stl` 用 three.js 转换为 `.glb` 再上传，存储的模型始终是 GLB，任何端（含 Unity）都能加载。
  - 涉及前端：`web/app/_utils/modelConvert.ts`（新增转换工具）、`web/app/resources/page.tsx`、`web/app/editor/resources/page.tsx`、`web/app/admin/three-courseware/_components/ModelSelector.tsx`。
- **历史数据**：之前已用 FBX 发布的课件需重新进编辑器「保存」一次（导出 GLB）或重新上传模型；新数据自动 GLB。

### 12.2 批量导入用户（含班级、学生）

- 入口：平台「用户管理」页 → **批量导入** 按钮（超管 / 校级管理员 / 教师可见）。
- 流程：下载 `.xlsx` 模板 → 填写（姓名、手机号必填；角色填「教师/学生」；班级不存在会自动创建）→ 上传，系统统一创建班级与用户。
- 接口：
  - `GET  /api/users/import-template` 下载模板
  - `POST /api/users/import`（multipart，字段 `file`；超管需带 `?schoolId=xxx`）
- 默认初始密码 `123456`；导入结果会逐行返回成功/失败原因。
- 依赖：后端新增 `xlsx`。

### 12.3 用户存储配额（默认 5GB，可单独调整）

- 新用户默认配额 **5GB**（`server/src/models/User.ts` 的 `DEFAULT_STORAGE_QUOTA`）。
- 上传资源时后端累加已用容量校验，**超额返回 413 并禁止上传**（普通上传 / STEP 转换 / 分块上传 / ZIP 解压均已拦截）。
- 校级管理员 / 超管可在「用户管理」页对单个教师/学生**调整配额**（设为 0 = 禁止上传）。
- 接口：
  - `GET /api/files/storage-usage` 当前用户用量
  - `PUT /api/users/:id/quota`（body `{ storageQuota: 字节 }`）
  - 用户列表接口已附带 `storageUsed` / `storageQuota`。
- 说明：已用容量按 `File` 集合实时聚合统计；课件编辑器临时 GLB、TTS 配音不计入。

### 12.4 三维编辑器资源管理：文件夹 + ZIP 自动解压

- 资源页（`web/app/editor/resources/page.tsx`）支持：**新建文件夹、面包屑导航、按目录浏览**；上传时归属当前目录。
- **上传 ZIP 自动解压**：解压后仅入库平台支持的格式（图片/视频/PDF/Office/glb/fbx/obj/stl），不支持的文件跳过。
  - 注：ZIP 解压**改为浏览器端处理**（见 12.4.2），压缩包内的 fbx/obj/stl 会自动转成 GLB 再入库，与单文件上传一致。
- 数据模型：新增 `Folder` 集合（`server/src/models/Folder.ts`）；`File` 增加 `folderId` 字段（null=根目录）。
- 接口：
  - `GET    /api/files/folders?parentId=` 列出子文件夹
  - `POST   /api/files/folder`（`{ name, parentId }`）新建
  - `PUT    /api/files/folder/:id` 重命名
  - `DELETE /api/files/folder/:id`（仅空文件夹可删）
  - `POST   /api/files/upload-zip`（multipart，字段 `file`，可带 `folderId`）
  - `GET /api/files/mine` 新增 `folderId` 参数（不传=全部，向后兼容；Unity 的 `/client/mine` 不受影响，仍返回全部资源）
- 依赖：后端新增 `adm-zip`。

#### 12.4.1 补充（同日二次更新）

- **上传可见性选择**：资源页（超管可见）新增「私有 / 公共」切换，决定本次上传归属个人资源还是平台公共素材（公共仅超管可传，沿用后端原有限制）。
  - `GET /api/files/mine` 新增 `visibility` 参数：`private`(默认) / `public` / `all`（本人全部）。资源页用 `all` 同时展示私有+公共并打标签。
- **拖拽移动资源**：在资源列表里把文件**拖到文件夹行**可移入；拖到上方**面包屑**（「全部资源」或上级目录）可移出。
  - 新接口：`PUT /api/files/:id/folder`（body `{ folderId }`，传 `root`/空 = 移到根目录）。
  - 公共资源也可被拥有者用文件夹归类（仅影响本人视图，`/public`、`/client/public` 仍按平铺返回）。

#### 12.4.2 ZIP 解压改为浏览器端（FBX 也转 GLB）

- 之前 ZIP 由后端解压，压缩包内的 FBX 会**原样入库不转换**，导致 Unity 仍无法加载。
- 现改为**浏览器端解压**（`web/app/_utils/modelConvert.ts` 的 `extractZipToFiles`，依赖 `fflate`）：解压后逐个把 fbx/obj/stl 转成 GLB，再调用 `POST /api/files/upload` 逐个上传（带当前文件夹与可见性）。不支持的文件跳过。
- 后端 `POST /api/files/upload-zip` 仍保留但前端不再使用。
- 依赖：前端新增 `fflate`（安装需 `--legacy-peer-deps`，与 Dockerfile 一致）。

> 备注：Unity 反馈的「个人资源列表无法获取」经确认为 Unity 客户端代码问题，平台 `/api/files/client/mine` 接口正常，本次未改动。

#### 12.4.3 ZIP 按层级建文件夹 + 多选批量 + 全类型预览（2026-06-01）

针对三维编辑器资源页（`web/app/editor/resources/page.tsx`）的三项体验优化：

1. **ZIP 解压保留目录层级**
   - 之前解压后所有资源都平铺到当前目录的最外层。现改为**按 ZIP 内部目录结构自动建文件夹**，资源放进对应子目录。
   - 实现：`extractZipToFiles`（`web/app/_utils/modelConvert.ts`）现返回每个文件在 ZIP 内的相对目录层级；上传时调用 `ensureFolderPath` 逐级「获取或创建」文件夹，再把文件上传到叶子目录。
   - 后端 `POST /api/files/folder` 改为**幂等**：同级已存在同名文件夹时直接返回已存在的（`existed:true`），便于重复导入与逐级建目录；如需重名报错可显式传 `strict:true`（手动新建仍保持原校验逻辑，按需）。

2. **名称多选 + 批量拖动/删除**
   - 资源列表名称前新增**复选框多选**（仅文件可选，文件夹不可选）。
   - 选中后可**一次性拖动**到目标文件夹/面包屑（批量移动），或点「批量删除」一次删除多个。
   - 切换目录时自动清空选择，避免误删其它目录文件。

3. **全类型在线预览（图片 / 模型 / 视频 / PDF）**
   - 之前预览用 `<img>` 直接指向受鉴权保护的下载地址，浏览器无法带 token，导致一律显示「此文件不支持预览」。
   - 现新增组件 `web/app/_components/ResourcePreview.tsx`：**先用带 token 的 fetch 把文件下成 blob**，再按类型渲染：
     - 图片 → `<img>`
     - 模型(glb/gltf) → **本地打包的 three.js（GLTFLoader + OrbitControls）离线渲染**，不依赖外网 CDN（学校内网可用），可旋转/缩放查看。
     - 视频 → `<video controls>`；PDF → `<iframe>`。
   - 说明：内网无法访问 `unpkg` 等 CDN，所以模型预览没有沿用 `/resources/viewer/model` 的 `<model-viewer>` 方案，改为本地 three.js 自建查看器。

> 部署：仅前后端源码改动，无新增依赖。`docker compose build server web && docker compose up -d server web` 即可。

#### 12.4.4 FBX/OBJ 转 GLB 贴图丢失修复（2026-06-01）

现象：带贴图的 FBX（如卷尺，贴图为外部 `卷尺.jpg`）转成 GLB 后预览只有底色、没有贴图。

原因与修复（`web/app/_utils/modelConvert.ts`）：

1. **外部贴图无法定位**：原来 `FBXLoader.parse(buf, '')` 资源路径传空，FBX 引用的外部贴图找不到。
   - 现 ZIP 解压时把包内**所有文件按文件名建「资源表」**传入转换器；用 `LoadingManager.setURLModifier` 按 basename（忽略大小写）把贴图引用映射到 ZIP 内对应图片，再由 `GLTFExporter(embedImages)` 嵌入 GLB。
2. **贴图异步加载竞态**：原来 parse 完立即导出，贴图可能还没加载完（内嵌贴图也会丢）。
   - 现导出前 `waitForTextures` **等所有贴图图片加载完成**（带 20s 超时兜底）再导出。
3. 顺带支持 **OBJ 的 `.mtl` 材质**：ZIP 内带 `.mtl` 时用 `MTLLoader` 解析并应用，贴图同样走资源表解析。

> 注意：**单文件**上传 FBX 时若贴图是外部独立文件，平台拿不到贴图无法嵌入——这类模型请走「上传 ZIP」并把 fbx 与贴图图片一起打包；或使用**贴图已内嵌**的 FBX（内嵌贴图现已能可靠保留）。

> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web` 即可。

#### 12.4.5 Unity 资源包贴图绑定（2026-06-01 二次修复）

进一步发现：用户的压缩包是 **Unity 资源导出**（含 `卷尺.fbx`、`卷尺.jpg`、`卷尺.mat`、`金属.mat` 及各自 `.meta`）。这种包里 **FBX 自身不含贴图引用**，贴图是由 Unity 的 `.mat` 通过 **GUID** 绑定到 `.jpg`，所以仅靠 URLModifier（12.4.4）补不上贴图。

新增 Unity 感知的贴图绑定（`web/app/_utils/modelConvert.ts`）：

1. `parseMetaGuids`：解析所有 `.meta`，建立 `guid → 资源文件名` 映射（如 `卷尺.jpg.meta` 的 guid → `卷尺.jpg`）。
2. `parseMatTextures`：解析 `.mat`，按「材质名（=.mat 文件名）→ 主贴图（_MainTex/_BaseMap 的 guid）」建立映射。
3. `applyUnityTextures`：FBX 解析后按 **材质名 ↔ .mat 名** 把贴图补到对应材质；若整包只有一张贴图且只有一个待贴材质，则兜底直接套用。
4. `normalizeMaterials`：把 FBX 默认的 Phong 等材质统一转成 `MeshStandardMaterial`（保留贴图/颜色），确保 `GLTFExporter` 能可靠导出贴图。

> 使用建议不变：带贴图模型请整目录打包成 ZIP（fbx + jpg + mat + meta 一起）再上传，平台会自动把贴图嵌入 GLB。
>
> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web`。

#### 12.4.6 3ds Max 导出 FBX 贴图槽位修复（2026-06-01 三次修复）

控制台报错定位到真正根因：

```
THREE.FBXLoader: 3dsMax|maps|texmap_diffuse map is not supported in three.js, skipping texture.
THREE.FBXLoader: unknown material type "unknown". Defaulting to MeshPhongMaterial.
```

该 FBX 由 **3ds Max 导出**，漫反射贴图挂在 Max 专有槽位 `3dsMax|maps|texmap_diffuse` 上，three.js 的 `FBXLoader` 不识别该槽位、**直接跳过贴图**，导致材质上没有任何 map；且材质类型为 `unknown`，回退成 Phong。

加强 `applyUnityTextures`（`web/app/_utils/modelConvert.ts`）的绑定鲁棒性：

1. 先按「材质名 ↔ .mat 名」精确匹配贴图（原逻辑）。
2. 匹配不到时，若整包只有**一张漫反射贴图**（来自 `.mat` 的唯一贴图，或包内只有一张图片），则套到**顶点数最多的主体材质**上，并**排除被 `.mat` 标记为无贴图的材质（如金属）**，避免把贴图错贴到金属小件。
3. 控制台输出 `[modelConvert] 贴图绑定` 诊断信息（材质名、.mat 映射、已绑定数）便于排查。

> 贴图方向：补绑的贴图保持 `flipY = true`（FBX/3ds Max 的 UV 约定），`GLTFExporter` 会把竖直翻转烘焙进导出图片、UV 保持 glTF 标准，因此 GLB 在网页预览与 Unity 中均正确（早期误设 `flipY=false` 会导致贴图翻转）。
>
> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web`。

#### 12.4.7 多贴图 PBR 模型导出健壮性（2026-06-01 四次修复）

现象：上传含多张贴图（BC/N/R 等）的钻机 FBX 后模型不显示，控制台报：

```
THREE.GLTFExporter: No valid image data found. Unable to process texture.
```

定位：几何体其实加载成功（顶点数正常），但 FBX 引用了**压缩包内不存在的贴图**（如 `T_Branding04_DC.png`），FBXLoader 给材质挂了一张空 image 贴图；`GLTFExporter` 一旦遇到无效贴图就**中断整模型导出**，导致连模型都看不到。

修复（`web/app/_utils/modelConvert.ts`）：

1. **`sanitizeTextures`**：导出前清理所有 image 为空/尺寸为 0 的贴图槽位（置空），保证单张坏图不影响整模型与有效贴图的导出。
2. **缺图占位**：`LoadingManager` 对压缩包内找不到的贴图返回 1×1 透明 PNG，避免向页面发起 404；并 `console.warn` 提示缺哪张图。
3. **PBR 多通道按名绑定**：按文件名后缀（`_BC/_BaseColor/_Albedo`、`_N/_Normal`、`_R/_Roughness`、`_M/_Metallic`、`_AO`、`_Emissive`）分组，再用「贴图组名 ↔ 材质名」包含匹配，分别绑定 `map / normalMap / roughnessMap / metalnessMap / aoMap / emissiveMap`（数据贴图用线性色彩空间）。
4. **空几何体保护与诊断**：转换前打印 `[modelConvert] 模型统计`（网格数/顶点数/节点类型）；若几何体为空则报清晰错误；转换失败在控制台打印具体原因。

> 提示：FBX 内引用但未随包提供的贴图无法补全（仅以透明占位）。请把模型用到的全部贴图与 fbx 一起打包上传；或在 3ds Max（Babylon glTF 插件）/ Blender 中直接导出 `.glb` 再上传（glb 直通、最可靠）。
>
> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web`。

#### 12.4.8 大文件预览 `Failed to fetch` 修复（2026-06-01 五次修复）

现象：44MB 的 `钻机.glb` 已成功入库，但点击预览报 `Failed to fetch` + 控制台多条 `net::ERR_FAILED 200 (OK)`。

排查（用无头浏览器在页面内复现 fetch）：

- 服务器侧正常：`curl` 直连 `:4000` 与经 Next `:3001` 代理均完整返回 44304720 字节。
- 浏览器侧：`HEAD`、`Range bytes=0-1023` 均成功；但**完整 GET 后 `res.blob()` 失败**。
- 改用 `cache:'no-store'` + **流式 `response.body.getReader()` 逐块读入内存再 `new Blob([..])`** → 成功。

根因：**Chrome 对大响应执行 `Response.blob()` 时会触发磁盘缓存物化，写缓存失败即报 `net::ERR_FAILED / Failed to fetch`**；而逐块读到 JS 内存不受影响。

修复（`web/app/_components/ResourcePreview.tsx`）：预览下载改为 `fetch(url, { cache:'no-store' })` 后用流式 reader 把分片拼成 `Uint8Array`，再 `new Blob([buffer])` 生成 objectURL；无 `body` 流时回退 `res.blob()`。图片/模型/视频/PDF 预览均走此逻辑，大文件不再报错。

> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web`。

#### 12.4.9 多材质贴图命名前缀不匹配修复（2026-06-01 六次修复）

现象：`钻机` 模型几何体能正常显示，但**大量贴图未绑定**（导出的 GLB 中 `images:0 / textures:0`）。

排查（解析服务器上已生成的 `钻机.glb`）：模型含 **18 个命名清晰的材质**，如 `MI_RotaryDrill_Arm`、`MI_RotaryDrill_Cables`、`MI_RotaryDrill_Interior`、`MI_RotaryDrill_Exterior`、`MI_RotaryDrill_Drillbit` 等；贴图文件名为 `T_RotaryDrill_Arm_BC/N/R.png` 等。

根因：12.4.7 的 PBR 多通道绑定按「贴图组名 ↔ 材质名」做 `includes` 双向包含匹配，但材质前缀是 **`MI_`**（Material Instance）、贴图前缀是 **`T_`**（Texture），归一化后 `t_rotarydrill_arm` 与 `mi_rotarydrill_arm` 互不包含 → 匹配全部失败。

修复（`web/app/_utils/modelConvert.ts` 的 `applyUnityTextures` Pass 3）：匹配前先 **剥掉命名前缀 token**（`MI_/MAT_/M_/T_/TEX_/SM_/MESH_/INST_/OBJ_` 等）得到核心名再比对。例如 `MI_RotaryDrill_Arm` 与 `T_RotaryDrill_Arm_BC` 均归一化为 `rotarydrill_arm`，从而把 `_BC/_N/_R` 分别绑定到 `map/normalMap/roughnessMap`。无对应贴图的材质（如玻璃 `MI_GlassSmoked`）保持原样。

> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web`。

#### 12.4.10 法线/粗糙度贴图保留 + Unity 材质基础色识别（2026-06-01 七次更新）

两项增强（均在 `web/app/_utils/modelConvert.ts`）：

1. **PBR 贴图保留修复**：`normalizeMaterials` 把 FBX 的 Phong 材质转标准材质时，原先只拷贝 `map`/`normalMap`，导致 `roughnessMap`/`metalnessMap`（即 `_R`/`_M` 贴图）在标准化时被丢弃。现补齐拷贝 `roughnessMap`、`metalnessMap`、`bumpMap`、`displacementMap`、`normalScale`、`emissiveIntensity`，并在存在金属/粗糙度贴图时把对应系数置 1 让贴图驱动。法线贴图本就支持，此修复让整套 PBR（基础色+法线+粗糙度+金属度）都能正确导出。

2. **Unity 材质基础色识别**：新增 `parseMatColors` 解析 `.mat` 里的 `_BaseColor`/`_Color`（`_BaseColor` 优先），在 `applyUnityTextures` 的 Pass 4 把颜色应用到同名材质（`color.setRGB(...)`，存在 sRGB 色彩空间时按 sRGB 解释），透明度 `a<1` 时自动开启透明。此前 Unity 里调整的材质球基础颜色不会带过来，现在可识别。注：纯 FBX（3ds Max/Maya）里设置的漫反射颜色一直可通过 `material.color` 带过来，本次补的是 Unity `.mat` 链路。

> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web`。

#### 12.4.11 兼容 TGA 贴图（2026-06-01 八次更新）

现象：上传含 `T_Branding01_BC.tga` 的 FBX 资源包，转出的 GLB 仅几 KB、无贴图。

根因：① 图片白名单 `IMAGE_EXTS` 不含 `.tga`，贴图未被纳入绑定候选；② **浏览器原生 `<img>`（`TextureLoader` 底层）无法解码 TGA**，FBX 内部引用与我们的绑定路径都拿不到有效图。

修复（`web/app/_utils/modelConvert.ts`）：

1. `IMAGE_EXTS` 增加 `.tga`。
2. 新增 `transcodeTgaResources`：在模型转换前，用 three 的 `TGALoader.parse()` 把资源表里所有 `.tga` 解码为 RGBA，经 canvas `toBlob('image/png')` **就地转码为 PNG 字节并写回同一个 key**。这样 FBX 内部贴图引用与我们的 `applyUnityTextures` 绑定两条路径都统一走浏览器可解码的 PNG；浏览器按内容解码、忽略 `.tga` 后缀。
3. `TGALoader` 解出的数据为左上角原点，与普通 PNG 一致，导出的 GLB 贴图方向不会颠倒。用 `WeakSet` 标记避免同一资源表重复转码。

> 部署：仅前端改动、无新增依赖。`docker compose build web && docker compose up -d web`。

---

### 12.5 资源管理与存储策略明确（2026-06-03）

本次明确并落地资源管理的「公共资源 / 个人资源」与存储配额规则，统一前后端口径。

### 策略总览

| 角色 | 上传的资源归属 | 可见范围 | 存储限额 |
|---|---|---|---|
| 超管（`superadmin`，即「Admin」账号） | **公共资源**（也可自选私有） | 全平台所有用户可见、可下载，并可**基于这些模型创建三维课件** | **不限容量** |
| 校级管理员 / 教师 / 学生 | **个人资源（私有）** | **仅本人可见** | 默认 **5GB**（可由管理员单独调整） |

要点：

1. **只有超管上传的资源才能是「公共资源」**。前端上传时的「公共」可见性选项仅超管可见；后端在 `POST /api/files/upload`、`/upload-step`、`/chunk/init` 均校验 `role === 'superadmin'`，非超管传 `visibility=public` 一律 403。
2. **公共资源全员可获取**：`GET /api/files/public`、`/api/files/client/public`（Unity）对所有登录用户返回全部 `visibility:public` 资源；下载接口对公共资源放行。学生即可拉取公共模型并在三维课件 / 元宇宙中使用。
3. **其他用户的资源默认私有、仅自己可见**：非超管上传强制 `visibility:private`，仅在本人 `GET /api/files/mine` 中可见。
4. **超管不限容量**：后端 `getUserQuota` 对超管返回「不限容量」哨兵值，配额校验直接放行；前端「用户管理」列表与三维编辑器资源页对超管显示「不限容量」。
5. **公共资源不占用任何人的个人空间**：个人已用空间仅统计本人的 **私有** 资源（`getUsedStorage` 与用户列表用量聚合均加了 `visibility:'private'` 过滤）。超管上传的公共素材不计入其个人用量，也不会消耗任何用户的 5GB 配额。

### 涉及改动

- 后端：
  - `server/src/utils/storage.ts`：新增 `UNLIMITED_QUOTA` 哨兵；`getUsedStorage` 仅统计私有资源；`getUserQuota` 对超管返回不限容量；`checkQuota` 不限容量时直接放行。
  - `server/src/routes/files.routes.ts`：`GET /api/files/storage-usage` 对超管返回 `{ quota:null, remaining:null, unlimited:true }`。
  - `server/src/controllers/users.controller.ts`：用户列表的 `storageUsed` 聚合仅统计私有资源。
- 前端：
  - `web/app/editor/resources/page.tsx`：配额卡片在不限容量时展示「已用 X（管理员账号不限容量…）」，不显示进度条。
  - `web/app/users/page.tsx`：用户列表存储列对超管显示「已用 / 不限容量」。

> 部署：前后端均有改动、无新增依赖。
> 后端：`docker compose build server && docker compose up -d server`（或按 5.2 用 `rebuild_server.sh`）。
> 前端：`docker compose build web && docker compose up -d web`（按 5.3 流程）。

### 12.6 模型封面自动截图 + 卡片视图（2026-06-03）

让资源（尤其是三维模型）以**封面卡片**展示，无需人工上传截图。

- **首次上传自动截图**：模型（统一为 `.glb`）上传成功后，前端用 three.js **离屏渲染一张 PNG 截图**作为封面，再调用 `POST /api/files/:id/cover` 存到服务器（单文件上传与 ZIP 解压上传均生效）。封面是资源的**附属文件**，存于 `storage/thumbnails/<fileId>.png`，**不入 File 集合、不计入个人配额**。
- **可手动更换**：资源页卡片上「更换封面」可上传自定义图片覆盖（仅拥有者/超管）。
- **卡片视图**：三维编辑器资源页（`web/app/editor/resources/page.tsx`）新增「卡片 / 列表」切换，卡片显示封面、类型、可见性、大小，支持预览/更换封面/删除、多选与拖拽移动。
- **接口返回封面地址**：`GET /api/files/mine`、`/api/files/public`、Unity 的 `/api/files/client/*` 每行新增 `thumbnailUrl`（无封面为 `null`）。
- **免登录封面接口**：`GET /api/files/:id/cover` 直接返回封面图片（带缓存），便于其它系统 `<img>` 引用。详见《平台登录接口-API文档.md》4.7。

涉及改动：

- 后端：`server/src/models/File.ts`（新增 `thumbnailRelPath`）、`server/src/routes/files.routes.ts`（新增 `POST/GET /api/files/:id/cover`、列表返回 `thumbnailUrl`、删除时清理封面）。
- 前端：新增 `web/app/_utils/modelThumbnail.ts`（GLB→PNG 截图）、`web/app/editor/resources/page.tsx`（自动生成封面、卡片视图、更换封面）。

> 部署：前后端均有改动、无新增依赖。后端 `docker compose build server && docker compose up -d server`；前端 `docker compose build web && docker compose up -d web`。

---

### 12.7 AI 文生图上线（豆包 Seedream 5.0）（2026-07-21）

为 AI 课件增加「AI 自动配图」能力：根据课程名称、段落标题、讲稿、图片关键词等上下文，先用豆包 LLM 生成英文提示词，再调用豆包 Seedream 5.0 文生图模型生成配图，作为步骤配图插入大纲。

### 新增接口

- `POST /api/ai/generate-image`（鉴权：超管 / 校管 / 教师）
  - body：`{ coursewareName, segmentTitle, say, imageKeywords, prompt?, size? }`
  - 不传 `prompt` 时，后端先用豆包 LLM 根据上下文生成英文提示词；再调用 Seedream 文生图（默认 `2048x2048`）。
  - 返回：`{ success, result: { url, prompt, title, source: 'AI生成 (豆包 Seedream)' } }`

### 涉及改动

- 后端：
  - `server/src/controllers/ai.controller.ts`：新增 `generateImage`。
  - `server/src/routes/ai.routes.ts`：新增 `POST /generate-image` 路由。
  - `server/src/utils/ai-services.ts`：新增 `generateImageWithDoubao`（调用 Seedream）、`generateImagePromptWithDoubao`（LLM 生成提示词）。
  - `server/src/config/env.ts`：新增 `doubaoImageModelId`、`doubaoThinking` 两个配置项。
- 前端：
  - 新增 `web/app/_components/AIProcessingModal.tsx`：AI 处理时的全屏动效弹窗（脉冲光环 / 旋转光晕 / 进度条 / 动态提示文字），用于 AI 课件生成、文生图等长耗时操作的视觉反馈。
  - AI 课件编辑器相关页面接入该弹窗与文生图入口。

### 新增环境变量（已写入服务器 `.env` 与 `docker-compose.yml`）

| 变量 | 值（服务器现状） | 说明 |
|---|---|---|
| `DOUBAO_IMAGE_MODEL_ID` | `doubao-seedream-5-0-260128` | 豆包文生图模型 ID（Seedream 5.0）。需在火山方舟控制台开通该模型 |
| `DOUBAO_THINKING` | `disabled` | 豆包 Seed 系列思考模式开关：`disabled`=关闭思考（快，默认）/ `enabled`=开启思考（质量高但慢） |
| `DOUBAO_MODEL_ID` | `ep-20260526174626-xrj4q`（**保持不变**） | 豆包文本/多模态模型。本次未改动，仍用现有 Doubao-Seed-2.0-lite 接入点 |

> 注：新代码默认 `DOUBAO_MODEL_ID=doubao-seed-2-1-pro-260628`、`DOUBAO_THINKING=disabled`、`DOUBAO_IMAGE_MODEL_ID=doubao-seedream-5-0-260128`。服务器 `.env` 显式覆盖了 `DOUBAO_MODEL_ID` 以沿用现有接入点（最稳/最省），其余两项与默认一致。

### 部署过程（本次实际执行）

1. 更新 `.tools/deploy-pkg/docker-compose.yml`：在 server.environment 补 `DOUBAO_THINKING`、`DOUBAO_IMAGE_MODEL_ID` 两行（引用 `.env`，带默认值）。
2. 上传 `docker-compose.yml` → `/data/yf-platform/`；服务器 `.env` 先备份再追加两行（`cp -a .env .env.bak.<时间戳>`，`grep -q` 判重后 `echo >>`）。
3. `docker compose config --quiet` 校验通过。
4. 后端：`tar` 打包 `server`（排除 node_modules/dist）→ SFTP 上传 → 执行 `rebuild_server.sh`（替换源码 + `docker compose build server` + `up -d server`），约 56 秒，`Server listening on port 4000`。
5. 前端：`tar` 打包 `web`（排除 node_modules/.next）→ SFTP 上传 → `rm -rf web && tar -xzf` → `rebuild_web.sh`（后台 `nohup` build + up），约 3 分钟，`BUILD_EXIT=0 / UP_EXIT=0`，`Next.js Ready in 986ms`。
6. 验证：`web:200`、`server` 正常、`POST /api/ai/generate-image` 经 web 代理返回 `401`（路由已注册、需鉴权，非 404）；容器内 `printenv` 确认三个新/旧豆包变量取值正确。

### 部署注意事项（踩坑记录）

- **`.tools/*.sh` 脚本在 Windows 上是 CRLF 行尾**，直接 `ssh_upload_run.py` 上传后 `bash` 执行会报 `$'\r': command not found`、`set: -: invalid option`，导致 `cd /data/yf-platform` 失败、`docker compose` 找不到 compose 文件。
  - 解决：上传前把脚本转成 LF（`(.replace('\r\n','\n'))`），或在服务器上 `sed -i 's/\r$//' /tmp/rebuild_*.sh` 再 `bash`。
  - 本次用临时 Python 脚本上传 LF 版本执行；后续建议把 `.tools/*.sh` 统一存为 LF（`.gitattributes` 加 `*.sh text eol=lf`）。
- `rebuild_web.sh` 用 `nohup` 后台构建，日志与 `webbuild.done` 标记写在 `$HOME`；若通过 `sudo bash` 触发，`$HOME` 会变成 `/root`，故日志在 `/root/webbuild.log`、`/root/webbuild.done`（而非 `/home/yfadmin/`）。轮询构建状态时注意取对路径。

> 部署：前后端均有改动、无新增依赖。后端 `docker compose build server && docker compose up -d server`；前端 `docker compose build web && docker compose up -d web`。

---

### 12.8 修复：发布配音静默失败 & 三维模型 scale 保存丢失（2026-07-22）

**问题1：发布时"秒发布、根本没有配音"**
- 根因：默认 TTS 供应商为豆包，而学校服务器 `.env` 未配置 `DOUBAO_TTS_APP_ID` / `DOUBAO_TTS_ACCESS_KEY`（豆包语音需在火山引擎"豆包语音"控制台独立开通）。`generateTTSWithDoubao` 在未配置时返回**假成功**（`success:true` 但无 `audioData`），导致 `batchGenerateTTSForCourse` 拿不到音频数据、静默跳过；`publish.controller` 又 catch 掉错误 → 课程秒级发布、无任何配音。
- 服务器现状：`DOUBAO_TTS_*` 缺失；`AZURE_SPEECH_KEY` + `AZURE_SPEECH_REGION=eastasia` 已完整配置（Azure TTS 可直接用）；`MINIMAX_API_KEY` 已配置。

修复内容：
- 后端 `server/src/utils/ai-services.ts`：`generateTTSWithDoubao` / `generateTTSWithAzure` 在未配置时改为返回 `success:false` + 明确错误信息（不再返回假成功的远程铃声占位）。
- 后端 `server/src/utils/ai-services.ts`：`batchGenerateTTSForCourse` 返回 `{ results, error }`，收集首个失败原因。
- 后端 `server/src/controllers/publish.controller.ts`：统计 `expected/generated`，配音全部或部分失败时在响应中返回 `ttsWarning`（不阻止发布，但不再静默）。
- 后端 `server/src/controllers/ai.controller.ts`：`getTTSProviders` 给每个供应商加 `configured` 标记，`recommendation.default` 改为"首个已配置的供应商"（学校服务器上自动推荐 Azure）。
- 前端 `web/app/admin/ai-course/[id]/components/PublishDialog.tsx`：加载供应商后默认选中推荐项；下拉标注"（未配置）"；发布返回 `ttsWarning` 时 `message.warning` 明确提示"改用 Azure TTS 或联系管理员配置豆包 TTS"。

> **二次排查补充**：首版修复后用户仍反馈"没有生成配音"。查 server 日志发现每次发布 `ttsConfig: undefined`、`hasTTSConfig: false`——**前端根本没发送 TTS 配置**（"生成配音文件"开关默认关闭，且用户未手动选音色），TTS 压根没执行，这才是"秒发布无配音"的真正根因（非豆包假成功那条路径）。
> 端到端验证：用超管 token 调 `/api/ai/tts/providers` 返回 `minimax(configured,12音) / doubao(未配置,8音) / azure(configured,67音)`，`recommendation.default=azure`；调 `/api/ai/tts`（provider=azure, voiceName=zh-CN-XiaoxiaoNeural）成功返回 ~17KB 真实音频 data URI。Azure TTS 链路完全可用。
> **最终前端修复**：`PublishDialog` 将 `enableTTS` 默认值由 `false` 改为 `true`；`loadTTSProviders` 自动选中"已配置且有音色"的供应商（优先服务端推荐顺序，跳过 `configured=false` 或无音色项）并自动选中其第一个音色。这样用户打开发布对话框即默认启用配音、自动选好 Azure + 音色，点"立即发布"即可生成配音，不再需要手动开启与选音色。用户仍可手动关闭或改选。

> 立即可用：发布时在"生成配音文件"里选 **Azure TTS**（已配置），即可正常生成配音。若要用豆包 TTS，需在 `.env` 补 `DOUBAO_TTS_APP_ID` / `DOUBAO_TTS_ACCESS_KEY` 并在 `docker-compose.yml` 的 server.environment 引用后重启 server。

**问题2：三维模型编辑时调整 scale 保存后刷新，模型仍很大**
- 根因：`needsGLBExport()` 只检测"删除/自定义动画/材质"变化，**不检测 TRS 变换（scale/position/rotation）变化**；而 TRS 轨道又刻意不写入 JSON（依赖 GLB 烘焙）。所以纯 scale 调整不触发 GLB 重新导出 → 保存后刷新丢失。另外导出时 `isTrivial` 会剥离"单子节点的 Group"包装层，若用户缩放的是根 Group，scale 会被丢弃。

修复内容（`web/app/admin/three-courseware/_components/ModelEditor3D.tsx`）：
- 新增 `transformModifiedRef`：gizmo `objectChange`、PRS 输入框（Px/Py/Pz/Rx/Ry/Rz/Sx/Sy/Sz）、批量"对齐到首个"任一触发即置真。
- 新增 `hasTransformChanges()` 并加入 `needsGLBExport()`，使变换变化触发 GLB 重新导出（scale 随之烘焙进新 GLB）。
- 加载新模型与 GLB 导出成功后重置 `transformModifiedRef`。
- `exportCurrentModelAsGLB` 的 `isTrivial` 增加"非单位变换不剥离"判断，保护带 scale 的根节点不被丢弃。

**部署过程（本次实际执行）**
1. 新增 `.tools/deploy_fix.py`：本地 `tarfile` 打包 server/web（归档路径带 `server/`、`web/` 顶层前缀，排除 node_modules/dist/.next/.git），SFTP 上传 `server-src.tar.gz`、`web-src.tar.gz` 及 LF 版 `rebuild_server.sh`、`rebuild_web.sh` 到 `/data/yf-platform/`；SSH 前台执行 `rebuild_server.sh`，后台执行 `rebuild_web.sh` 并轮询 `/root/webbuild.done`。
2. `rebuild_server.sh` 增加根目录散落文件清理（清理上次错误解压残留的 `Dockerfile.deploy`/`src`/`package.json` 等）；`rebuild_web.sh` 增加 `rm -rf web && tar -xzf web-src.tar.gz` 解压步骤（此前缺失导致 web 构建命中缓存、源码未更新）。
3. 结果：server `Source replaced` → 镜像构建 → `Server listening on port 4000`（rc=0）；web `BUILD_EXIT=0`、`UP_EXIT=0`。三容器均 Up。
4. 验证：server 容器 `dist/utils/ai-services.js` 含"豆包TTS未配置"、`dist/controllers/ai.controller.js` 含 `configured` 标记；`/api/ai/tts/providers` 返回 401（路由已注册、需鉴权）。

> 踩坑：`tar` 归档路径必须带 `server/`、`web/` 顶层前缀，否则远程 `tar -xzf` 后文件散到 `/data/yf-platform/` 根目录、`ls server/Dockerfile.deploy` 失败（`set -e` 直接退出，server 镜像不重建）。`rebuild_web.sh` 必须先解压 `web-src.tar.gz` 再 `docker compose build web`，否则命中旧缓存、web 源码不更新。

---

### 12.9 切换为仅豆包配音（停用 Azure / Minimax）（2026-07-22）

- 背景：用户提供本地豆包 TTS 凭据（`DOUBAO_TTS_APP_ID` / `DOUBAO_TTS_ACCESS_KEY`，取自本地根目录 `.env`），要求学校服务器只用豆包配音，停用另外两个供应商。
- 改动：
  1. **服务器 `.env`**：追加 `DOUBAO_TTS_APP_ID=5602051788`、`DOUBAO_TTS_ACCESS_KEY=***`（已存在则跳过，已自动备份 `.env.bak.<时间戳>`）。`docker-compose.yml` 的 server.environment 早已引用 `DOUBAO_TTS_APP_ID` / `DOUBAO_TTS_ACCESS_KEY`，故重启即注入容器。
  2. **后端 `server/src/controllers/ai.controller.ts` `getTTSProviders`**：移除 Minimax、Azure 两个供应商及其音色列表与 `getAzureVoices` 调用，**只返回豆包**（`configured=true`、8 个 `*_uranus_bigtts` 音色）；`recommendation.default` 固定为 `doubao`。同步从 import 中删除已不再使用的 `getAzureVoices`。单条 TTS 预览接口 `/api/ai/tts` 的 minimax/azure 分支代码保留但前端不再可选，实际不会被触发。
  3. 前端无需重新部署：`PublishDialog` 已实现"自动选中第一个已配置且有音色的供应商"，providers 列表只剩豆包后会自动选中豆包 + 首个音色（小何 2.0）。
- 部署：新增 `.tools/deploy_server_doubao.py`（打包上传 server 源码 → 写入豆包凭据到 `.env` → 执行 `rebuild_server.sh`）。结果 `Server listening on port 4000`（rc=0）。
- 验证：
  - 容器 `printenv`：`DOUBAO_TTS_APP_ID=5602051788`、`DOUBAO_TTS_ACCESS_KEY=***` ✓
  - `/api/ai/tts/providers`：`providers=[('doubao', True, 8)]`、`default='doubao'`（Azure/Minimax 已消失）✓
  - `/api/ai/tts`（provider=doubao, speaker=`zh_female_xiaohe_uranus_bigtts`）：返回真实音频 data URI（~32.8KB，时长 2.6s）✓
- 注意：豆包 2.0 音色必须用 `seed-tts-2.0` 资源下的 `*_uranus_bigtts` 系列；若误用其他系列（如 `zh_male_M392_conversation_wvae`）会报 `resource ID is mismatched with speaker related resource`。前端音色下拉已限定为列表内音色，不会触发该错误。

---

## 附录 A：豆包接入点管理

火山方舟控制台：https://console.volcengine.com/ark

- **当前账户 UID**：`2105288655`
- **API Key**：`d7eba3fb-f58c-4cc7-8921-ca5227be4f96`
- **当前接入点**：`ep-20260526174626-xrj4q`（Doubao-Seed-2.0-lite，按 Token 付费）

更换接入点的步骤：

1. 控制台 → 模型广场 → 开通需要的模型
2. 控制台 → 在线推理 → 自定义推理接入点 → 创建 → 关联模型
3. 拿到新的 `ep-xxxxxxxx`
4. 改 `/data/yf-platform/.env` 里的 `DOUBAO_MODEL_ID=<新ID>`
5. `docker compose up -d --no-deps server`（不需重 build，只是换环境变量）

## 附录 B：联系信息

| 角色 | 联系方式 |
|---|---|
| 学校运维 | （待补充） |
| 平台开发负责人 | （待补充） |
| 火山方舟账号 | 主账号手机：13564719755 |

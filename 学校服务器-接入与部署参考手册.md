# 水利水电职业技术学院 — 服务器接入与部署参考手册

> 用途：描述这台学校服务器的**硬件/系统/网络情况**与**连接方式**，作为在本机上部署**任意新项目**的通用参考。
> 本文档**不含任何具体业务项目内容**，只讲服务器本身与通用部署约定。
> 创建时间：2026-06

---

## 一、服务器基本信息

### 1.1 物理 / 虚拟资源

| 项目 | 值 |
|---|---|
| 宿主机（ESXi） | 学校 ESXi03（`172.17.136.112`） |
| 虚拟机名称 | `YF-Platform` |
| 操作系统 | Ubuntu 24.10（**已 EOL，使用 old-releases 源**；后续建议迁 24.04 LTS） |
| CPU | 8 核 |
| 内存 | 32 GB |
| 系统盘 | `/dev/sda` 200 GB（挂 `/`，约 195 GB） |
| 数据盘 | `/dev/sdb` 800 GB（挂载到 `/data`） |
| IP（静态） | `172.17.136.200/24` |
| 网关 | `172.17.136.1` |
| DNS | `192.168.103.87`、`192.168.101.87` |

> 这是一台**内网**服务器，仅在校园网 `172.17.x.x` 段内可访问。

### 1.2 运行环境（已装好，可直接用）

| 组件 | 说明 |
|---|---|
| Docker Engine + Compose 插件 | 已安装，`yfadmin` 已加入 `docker` 组，免 sudo 跑 docker |
| Docker data-root | 已指向 `/data/docker`（镜像/层数据落在数据盘） |
| 镜像加速 | `/etc/docker/daemon.json` 已配 daocloud / 1panel / rat.dev 三个加速器 |
| 时区 | `Asia/Shanghai` |

---

## 二、如何连接服务器

### 2.1 SSH 直连（最常用）

```bash
ssh yfadmin@172.17.136.200
# 密码: 88888888
```

登录后即可用 `docker`、`docker compose`（无需 sudo）。

> ⚠️ 生产环境建议：① 改默认密码（`passwd`）；② 配置 SSH 公钥免密；③ 关闭密码登录（`/etc/ssh/sshd_config`）。

### 2.2 从 Windows 用脚本远程操作（自动化部署）

仓库 `.tools/` 目录里有三个基于 Python + paramiko 的小脚本，连接信息已内置（`172.17.136.200` / `yfadmin` / `88888888`）。在任意项目里都能直接复用：

| 脚本 | 作用 | 用法 |
|---|---|---|
| `sftp_upload.py` | 上传文件到服务器某目录 | `python sftp_upload.py <本地文件...> <远程目录>` |
| `ssh_run.py` | 在服务器执行一条命令（支持 `--sudo`） | `python ssh_run.py "<命令>" [--sudo]` |
| `ssh_upload_run.py` | 上传一个 sh 脚本并立即执行 | `python ssh_upload_run.py <本地脚本> <远程路径>` |

依赖：`pip install paramiko`。脚本核心连接参数：

```python
HOST = "172.17.136.200"
PORT = 22
USER = "yfadmin"
PASS = "88888888"
```

> PowerShell 不支持 `&&` 连接命令，多条命令请分开执行或用 `;`。

### 2.3 Web 管理界面

| 用途 | 地址 | 账号 |
|---|---|---|
| Portainer（图形化管 Docker） | `http://172.17.136.200:9000` | 首次访问自行设置 |
| MinIO 控制台（对象存储） | `http://172.17.136.200:9101` | `yfadmin` / `yfPlatform@2026` |

---

## 三、关键凭据（请妥善保管）

| 服务 | 用户名 | 密码 |
|---|---|---|
| Linux 系统 | `yfadmin` | `88888888` |
| MinIO 控制台 / S3 | `yfadmin` | `yfPlatform@2026` |
| Portainer | 首次访问自行设置 | — |

---

## 四、目录结构约定（重要！）

所有数据都落在 800 GB 数据盘 `/data` 上，**系统盘只放系统**。

```
/data
├── docker/                 # Docker 自身镜像/层数据（已设为 data-root）
├── infra/                  # 基础设施 compose（每个服务一个目录）
│   ├── portainer/docker-compose.yml
│   └── minio/docker-compose.yml
├── storage/                # 各项目持久化数据卷
│   ├── minio/              # MinIO 对象存储数据
│   └── <项目名>/           # 新项目的数据卷放这里
└── <项目名>/               # 新项目的代码 + docker-compose.yml
```

**铁律**：任何要持久化的容器卷一律挂到 `/data/storage/<服务名>/`，**绝不用 docker named volume**（避免数据落到默认路径找不到）。

---

## 五、已占用端口（新项目避开）

| 已占用端口 | 服务 |
|---|---|
| 22 | SSH |
| 80, 443 | 保留给将来的 nginx 反代 |
| 3001 | 已有项目 web |
| 4000 | 已有项目 server |
| 9000, 9443 | Portainer |
| 9100, 9101 | MinIO |
| 27017 | MongoDB（仅容器内部） |

**新项目尽量从 80xx / 30xx / 50xx 段挑空闲端口。**

确认端口是否被占用：

```bash
ss -tlnp | grep :<端口>
docker ps --format '{{.Names}}\t{{.Ports}}'
```

---

## 六、新项目部署模板

### 6.1 建目录

```bash
sudo mkdir -p /data/<项目名>           # 项目代码 + compose
sudo mkdir -p /data/storage/<项目名>   # 项目数据卷
sudo chown -R yfadmin:yfadmin /data/<项目名> /data/storage/<项目名>
```

### 6.2 docker-compose.yml 模板

```yaml
services:
  app:
    image: <镜像>            # 或 build: ./app
    container_name: <项目名>-app
    restart: unless-stopped
    environment:
      - KEY=${KEY}           # 引用 .env 里的变量
    volumes:
      - /data/storage/<项目名>/app-data:/app/data
    ports:
      - "<外部端口>:<内部端口>"
    networks: [<项目名>-net]

networks:
  <项目名>-net:
    driver: bridge
```

### 6.3 国内网络镜像源（内网必备）

Node 项目，Dockerfile 里 `npm install` 之前加：

```dockerfile
RUN npm config set registry https://registry.npmmirror.com
```

Python 项目：

```dockerfile
RUN pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

镜像 pull 不动时，把 tag 换成加速器路径，例如：

```yaml
image: docker.m.daocloud.io/library/mongo:6
```

### 6.4 启动

```bash
cd /data/<项目名>
docker compose up -d
docker compose ps
```

---

## 七、运维常用命令

```bash
# 状态
docker ps                              # 全部容器
docker stats --no-stream               # CPU / 内存
df -hT /data /                         # 磁盘

# 日志
docker compose logs -f <服务名>        # 实时跟随
docker compose logs --tail=100 <服务名>

# 重启 / 停止
docker compose restart <服务名>
docker compose up -d --force-recreate  # 重建容器（不重 build）
docker compose down                    # 停止并删除容器（数据保留）

# 进容器排错
docker compose exec <服务名> sh
docker compose exec <服务名> printenv

# 磁盘紧张时清理
docker image prune -a -f
docker builder prune -a -f
docker system df
```

---

## 八、故障排查要点

### 8.1 容器起不来

```bash
docker compose logs <服务名>           # 看启动日志
docker compose config                  # 看渲染后的最终配置
docker inspect <容器名>                # 看挂载/环境变量
```

### 8.2 镜像 pull 不下来

`/etc/docker/daemon.json` 已配三个加速器（daocloud、1panel、rat.dev）。仍失败就把镜像 tag 改成对应加速器路径（见 6.3）。

### 8.3 `apt update` 报 404

Ubuntu 24.10 已 EOL，**只能用 `old-releases.ubuntu.com`**，源文件 `/etc/apt/sources.list.d/ubuntu.sources` 已配好。**不要切回 archive.ubuntu.com 或 aliyun**，它们已无 oracular 包。

### 8.4 磁盘满了

优先排查 `/data/docker`（镜像/构建缓存）与 `/data/storage`（数据卷）。先 `docker system df` 看占用，再按需 `prune`。

---

## 九、从零重建服务器（灾备参考）

若需重装整台机器，关键步骤：

1. **ESXi 建 VM**：登录 `https://172.17.136.112` → CPU 8 / 内存 32 GB / 系统盘 200 GB + 数据盘 800 GB（**两块盘分开建，不要合并**）。
2. **装 Ubuntu**：网络手动配静态 IP `172.17.136.200/24`、网关 `172.17.136.1`、DNS `192.168.103.87,192.168.101.87`；磁盘只用 `/dev/sda`，`/dev/sdb` 不动；用户 `yfadmin`；**必勾 Install OpenSSH server**。
3. **初始化脚本**（要点）：
   - 切 APT 源到 `old-releases.ubuntu.com`（oracular，24.10 已 EOL）
   - 格式化挂载数据盘：`mkfs.ext4 -L data /dev/sdb` → 写 `/etc/fstab`（用 UUID）→ 挂到 `/data`
   - 装 Docker（清华源 `mirrors.tuna.tsinghua.edu.cn/docker-ce`，oracular 通道）
   - 配 `/etc/docker/daemon.json`：镜像加速器 + `"data-root": "/data/docker"`，然后 `usermod -aG docker yfadmin`
4. **建基础目录**：`/data/{docker,infra,storage}`，`chown` 给 `yfadmin`。
5. **起基础设施**：Portainer（`9000/9443`）、MinIO（`9100/9101`，账号见第三章）各自一个 compose 目录在 `/data/infra/` 下。

> 上述每一步的完整脚本与命令，可参考原始详尽版部署手册。

---

## 附录：联系信息

| 角色 | 联系方式 |
|---|---|
| 学校运维 | （待补充） |
| 部署负责人 | （待补充） |

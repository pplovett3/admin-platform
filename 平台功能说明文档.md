# 三维课件制作平台 — 功能说明文档

> 平台名称：CollabXR 三维课件制作平台  
> 技术栈：Next.js 14（前端）+ Express + MongoDB（后端）  
> 更新时间：2026-06-12

---

## 目录

1. [平台概述](#1-平台概述)
2. [角色权限体系](#2-角色权限体系)
3. [用户管理](#3-用户管理)
4. [资源管理](#4-资源管理)
5. [三维课件制作](#5-三维课件制作)
6. [AI 课件制作](#6-ai-课件制作)
7. [AI 课件查看（公开播放）](#7-ai-课件查看公开播放)
8. [学生门户](#8-学生门户)
9. [成绩与数据分析](#9-成绩与数据分析)
10. [课件审核与发布](#10-课件审核与发布)
11. [激活码系统](#11-激活码系统)
12. [Unity / 客户端对接](#12-unity--客户端对接)

---

## 1. 平台概述

CollabXR 是一个面向职业教育领域的**三维课件制作与 AI 数字人授课平台**。核心工作流如下：

```
上传 3D 模型 → 制作三维课件（标注/动画）→ 生成 AI 授课大纲 → 配置考题与 TTS 配音 → 审核发布 → 学生在线学习 / Unity 客户端播放
```

平台同时支持：
- 传统**虚拟仿真课程**（含模块化成绩、学习时长统计）
- **WebXR / VR** 沉浸式学习体验
- 多角色分级管理（超管 / 校级管理员 / 教师 / 学生）

---

## 2. 角色权限体系

| 角色 | 标识 | 权限范围 |
|------|------|----------|
| 超级管理员 | `superadmin` | 全平台所有数据，管理学校、配额、激活码 |
| 校级管理员 | `schoolAdmin` | 本校所有数据，管理本校教师与学生 |
| 教师 | `teacher` | 本校学生管理，制作课件，查看本班成绩 |
| 学生 | `student` | 查看已发布课程，参与学习与答题 |

- 登录方式：手机号 + 密码（JWT Token 认证）
- 默认密码：`123456`，可由管理员重置
- 教师和学生账号与「学校」和「班级」强绑定

---

## 3. 用户管理

**页面路径**：`/users`

### 3.1 用户列表与查询

- 列表展示用户姓名、手机号、角色、学校、班级、学号
- 多维度筛选：姓名 / 手机号 / 学号关键字、所属学校、所属班级
- Tab 分类展示：
  - 超管可见「校级管理员 / 教师 / 学生」三个 Tab
  - 校管可见「教师 / 学生」两个 Tab
  - 教师仅可见「学生」Tab

### 3.2 用户创建与编辑

- 新增用户时按角色限制填写字段（学校、班级、学号、密码）
- 教师创建的用户强制为 `student` 且绑定本校
- 编辑支持修改姓名、手机号、班级、密码等

### 3.3 存储配额管理

- 列表展示每个用户已用存储 / 配额（进度条可视化）
- 超管不受配额限制
- 管理员可调整用户配额（单位：GB），接口 `PUT /api/users/:id/quota`
- 配额超限时禁止上传新文件

### 3.4 批量导入

- 下载标准 xlsx 模板（含字段说明页）
- 上传 Excel 批量创建用户；超管需先选择目标学校
- 班级不存在时自动创建；逐行错误收集并返回失败明细

### 3.5 学校与班级管理（超管 / 校管）

- 超管管理所有学校（新增 / 编辑 / 删除）
- 校管及超管管理班级（按学校 / 班主任筛选）

---

## 4. 资源管理

### 4.1 编辑器资源库（增强版）

**页面路径**：`/editor/resources`

#### 文件夹管理
- 支持**虚拟文件夹**树形结构，面包屑导航
- 新建 / 重命名 / 删除文件夹
- 拖拽将文件移入不同文件夹

#### 文件上传
- 支持类型：视频、图片、PDF、PPT、Word、3D 模型（FBX / OBJ / STL / GLB / STEP）
- **大文件分块上传**：超过 80 MB 自动切分（50 MB / 块），分 init → chunk → complete 三段式上传
- **ZIP 解压上传**：自动解压并批量入库
- **3D 模型格式转换**：FBX / OBJ / STL 自动转换为 GLB 格式
  - 可配置缩放倍数（默认 ÷1000 适配常见建模软件单位）
  - FBX 支持朝向校正（绕 X 轴 -90°）
- **STEP 模型转换**：上传 STEP 文件，服务端调用转换器生成 GLB

#### 封面管理
- GLB 模型上传后自动截取缩略图（`captureGlbThumbnail`，不计入存储配额）
- 支持手动替换封面图片

#### 文件操作
- 列表 / 卡片视图切换
- 多选批量删除
- 图片 / 视频在线预览
- GLB 模型跳转独立查看器页面（`/resources/viewer/model`）
- 文件下载

#### 存储配额
- 顶部显示已用 / 总配额进度条（调用 `/api/files/storage-usage`）

---

### 4.2 公共资源管理（简化版）

**页面路径**：`/resources`

- 双 Tab：「我的资源」/ 「公共资源」
- 超管可将文件设置为 `visibility: public`（公开资源）
- 功能与编辑器资源库基本一致，不含文件夹管理

---

## 5. 三维课件制作

**页面路径**：`/admin/three-courseware` 或 `/editor/three-courseware`

### 5.1 课件列表管理

- 课件卡片展示（封面缩略图、名称、描述、状态）
- 搜索、编辑元信息（名称 / 描述）、删除
- 状态标识：草稿 / 待审核 / 已通过 / 已拒绝 / 已下架

### 5.2 新建课件

- 关联已上传的 3D 模型文件
- 填写课件名称与描述

### 5.3 三维课件编辑器（核心功能）

**页面路径**：`/admin/three-courseware/[id]`（全屏编辑器）

编辑器基于 **Three.js** 构建，主要包含以下子功能：

#### 模型加载与查看
- 支持加载 GLB / FBX / OBJ 格式模型
- 模型节点树展示（可折叠 / 展开 / 显隐切换）
- 轨道控制（旋转、缩放、平移）
- 场景背景 / 环境光 / 材质参数调整

#### 标注（热点）系统
- 在模型任意位置添加**标注热点**（Annotation / Hotspot）
- 每个热点包含：名称、描述文本、关联媒体（图片 / 视频）
- 热点点击后展示详情弹窗

#### 动画时间线编辑
三条独立轨道，精确控制每一帧的课件状态：

| 轨道类型 | 功能 |
|----------|------|
| **相机动画轨道** | 关键帧记录相机位置 / 朝向 / 焦点，制作流畅的镜头切换动画 |
| **显隐轨道** | 控制模型各节点在不同时间帧的显示 / 隐藏状态，用于分解展示 |
| **变换轨道** | 控制模型节点在不同帧的位置 / 旋转 / 缩放变换 |

#### 步骤编辑
- 课件按「步骤（Step）」组织内容，每步对应一段说明
- 每步可关联标注、相机视角、模型状态

#### AI 辅助功能
- **模型结构整理**（QwenVL 视觉大模型）：自动识别并重命名模型节点
- **单零件识别**：AI 分析单个节点外观并命名
- **标注摘要生成**：基于已有标注自动生成课件摘要

#### 保存与封面
- 编辑后的模型数据（动画 / 标注 / 修改几何体）保存为 `modified.glb` 文件
- 自动截取当前视图作为课件封面缩略图

---

## 6. AI 课件制作

**页面路径**：`/admin/ai-course` 或 `/editor/ai-course`

### 6.1 课程列表

- AI 课件卡片展示（封面、标题、关联三维课件、状态）
- 草稿 / 已发布状态标识，支持搜索与删除

### 6.2 新建 AI 课件

- 填写课程名称、描述
- 关联已制作好的**三维课件**（必选）

### 6.3 AI 课件编辑器（主页面）

**页面路径**：`/admin/ai-course/[id]`（全屏编辑器）

编辑器采用左右分栏布局：

#### 左侧：大纲编辑器（`OutlineEditor`）
- 课程内容按「段落 → 步骤」两级结构组织
- 每个步骤支持多种内容类型：
  - `text`：文字说明
  - `image`：图片配图
  - `video`：视频片段
  - `3d-view`：关联 3D 模型特定视角
- 段落 / 步骤支持**拖拽排序**

#### 右侧：属性面板（`PropertyPanel`）
- 编辑选中步骤的详细属性（标题、正文、配图、TTS 文本等）
- 实时关联右侧 3D 预览视角

#### AI 功能（DeepSeek 大模型）
- **一键生成授课大纲**：根据课件名称和关联模型自动生成完整大纲
- **AI 生成考题**：自动为每个段落生成配套习题

#### 考题编辑（`QuestionEditor`）
- 支持两类题型：
  - **理论题**：选择 / 判断题，用于知识点检验
  - **互动题**：关联 3D 模型节点，要求学生点击正确零件

#### TTS 语音配置
- 支持多 TTS 引擎：Minimax、Azure
- 可选择音色（男声 / 女声）
- 发布时可**批量为所有步骤生成音频**（`batchGenerateTTSForCourse`）

#### 预览播放
- 编辑器内置预览播放器（`CoursePreviewPlayer`），可随时预览完整授课效果

### 6.4 发布课件

- 填写发布配置（封面、简介、公开设置）
- 可选择是否批量生成 TTS 音频
- 发布后生成**公开访问链接**（`/course/[publishId]`）
- 发布内容作为快照（`PublishedCourse`）存储，修改原课件不影响已发布版本

---

## 7. AI 课件查看（公开播放）

**页面路径**：`/course/[publishId]`（无需登录）

### 7.1 三种播放模式

| 模式 | 说明 |
|------|------|
| **学习模式** | 按大纲逐步播放，数字人授课，同步 3D 视角切换 + 音频 + 字幕 |
| **探索模式** | 自由浏览 3D 模型，查看各热点标注详情 |
| **答题模式** | 完成课程配套考题，提交后显示成绩 |

### 7.2 学习模式（`PublicCoursePlayer`）
- 按段落/步骤顺序播放
- 自动切换 3D 模型视角（相机动画回放）
- 同步控制模型节点显隐 / 变换
- TTS 音频播放 + 字幕展示
- 进度控制（上一步 / 下一步 / 跳转章节）

### 7.3 探索模式（`ModelExplorer`）
- 自由旋转/缩放 3D 模型
- 点击热点查看标注详情（文字 / 图片 / 视频）
- 模型节点树导航，点击高亮对应零件

### 7.4 答题模式（`QuizPlayer`）
- 理论题：单选 / 判断，选项高亮反馈
- 互动题：直接在 3D 模型上点击对应零件作答
- 提交答卷至 `/api/quiz/submit`，返回得分与解析
- 答题记录写入用户学习档案

### 7.5 WebXR / VR 支持
- 支持 WebXR 标准，可在 VR 头显中沉浸式体验课件
- 包含 VR 控制器交互、空间传送、UI 面板等
- 自动检测设备能力，降级回退至普通 3D 模式

### 7.6 分享与统计
- 发布链接无需登录即可访问
- 每次访问自动累计 `viewCount`（访问次数）
- 可生成分享二维码

---

## 8. 学生门户

**页面路径**：`/portal`（独立布局，非管理后台）

### 8.1 课程广场

- 展示所有审核通过且已发布的课件（三维课件 + AI 课件）
- 支持关键字搜索
- 点击进入对应课程学习页

### 8.2 我的学习

- 查看个人答题记录（课程名、得分、答题时间）
- 学习进度跟踪

### 8.3 元宇宙大厅

- 需管理员开启 `metaverseAllowed` 权限
- 通过 `/metaverse/authorize` 进行授权鉴权

---

## 9. 成绩与数据分析

### 9.1 成绩管理

**页面路径**：`/scores`

#### Tab 1：虚拟仿真课程成绩
- 支持「**简单课程**」（按学习时长计）和「**模块化课程**」（按模块得分计）
- **学生视角**：查看自己各模块最高分、历次提交记录
- **教师 / 校管视角**：班级成绩汇总表，点击查看单个学生的模块详情
- **超管视角**：额外支持按学校 / 班级筛选

#### Tab 2：AI 课程答题成绩
- 统计卡片：答题总次数、平均分、最高分、及格率
- 明细列表：课程名、得分、正确率、是否及格、答题时间

### 9.2 数据分析看板

**页面路径**：`/analytics`（教师 / 校管）、`/admin/analytics`（超管）

| 角色 | 可查看内容 |
|------|-----------|
| 超管 | 全平台学校数、用户数、课件数、访问量；趋势图 |
| 校管 | 本校各班级学习数据、课程完成率 |
| 教师 | 本班学生学习进度、成绩分布 |
| 学生 | 个人学习时长、答题统计 |

---

## 10. 课件审核与发布

### 10.1 审核流程

```
制作完成 → 提交审核（pending）→ 审核通过（approved）/ 驳回（rejected）→ [可下架（archived）]
```

- 教师 / 校管提交课件进入待审核队列
- 超管 / 校管在 `/admin/course-review` 页面审批
- 驳回时可填写原因，作者可修改后重新提交
- 下架后课件从公开列表移除，但数据保留

### 10.2 课件授权（虚拟仿真课程）

- 超管将虚拟仿真课程授权给指定学校（`Enrollment`）
- 学校未授权时，该校学生无法访问对应课程

---

## 11. 激活码系统

**页面路径**：`/admin/activation-codes`、`/admin/activations`

- 超管生成激活码（指定课程 / 有效期 / 可激活次数）
- 学生使用激活码在 `/activate` 页面激活指定课程权限
- 查看激活码使用记录（已激活学生、激活时间）
- 可手动撤销指定用户的激活状态

---

## 12. Unity / 客户端对接

平台提供面向 Unity 客户端的专用 API：

| 接口 | 说明 |
|------|------|
| `GET /api/published-courses/client/list` | 获取已发布 AI 课件列表（含封面、标题、publishId） |
| `GET /api/coursewares/client/list` | 获取三维课件列表（含模型 URL、标注数据） |
| `GET /api/public/course/:publishId` | 无需认证，获取完整课程数据（含音频 URL） |
| `GET /api/public/files/:fileId` | 无需认证的文件代理访问（供课件内资源引用） |

- 所有内部文件 URL 在对外接口中自动转换为公网代理地址
- Unity 客户端通过激活码系统验证用户课程访问权限

---

## 附：平台整体数据流

```
资源上传（File）
    ↓
三维课件制作（Courseware）：标注 + 时间线 + 步骤
    ↓
AI 课件制作（AICourse）：大纲 + 考题 + TTS
    ↓
提交审核 → 审核通过
    ↓
发布（PublishedCourse 快照）
    ↓
┌─────────────────────────────────────┐
│  公开播放 /course/:id（学习/探索/答题）│
│  学生门户 /portal                    │
│  Unity 客户端（API）                 │
└─────────────────────────────────────┘
    ↓
成绩记录（Quiz / Score / TimeLog）
    ↓
数据分析看板（Analytics）
```

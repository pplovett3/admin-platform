# Admin Platform 环境变量配置模板
# 复制此文件为 .env（本地开发）或 .env.prod（生产环境）并修改相应的值

# ==========================================
# 通用配置
# ==========================================

# JWT 密钥（必须修改！建议使用 32 位以上随机字符串）
# 生成方法：
#   Linux/Mac: openssl rand -base64 32
#   PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
#   Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=change_me_to_a_random_string_in_production

# ==========================================
# 后端 Server 配置
# ==========================================

# 服务端口（默认 4000）
PORT=4000

# MongoDB 连接字符串
# 开发环境（Docker Compose 内部）
MONGODB_URI=mongodb://mongo:27017/admin_platform

# 如果使用外部 MongoDB 或启用了认证，格式如下：
# MONGODB_URI=mongodb://username:password@host:27017/admin_platform

# 文件存储根目录
# Windows 开发环境示例
STORAGE_ROOT=Y:\\metaclassroom

# Linux/NAS 生产环境示例
# STORAGE_ROOT=/volume1/metaclassroom

# ==========================================
# 前端 Web 配置
# ==========================================

# 后端 API 地址（必须配置！）
# 本地开发
NEXT_PUBLIC_API_URL=http://localhost:4000

# 生产环境示例（使用 IP）
# NEXT_PUBLIC_API_URL=http://106.15.229.165:4000

# 生产环境示例（使用域名）
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# ==========================================
# MongoDB 配置（可选）
# ==========================================

# 如果需要启用 MongoDB 认证，取消注释并设置
# MONGO_INITDB_ROOT_USERNAME=admin
# MONGO_PASSWORD=your_strong_mongodb_password

# ==========================================
# 阿里云镜像仓库配置（用于脚本）
# ==========================================

# 镜像仓库地址
REGISTRY=registry.cn-hangzhou.aliyuncs.com

# 命名空间（在阿里云容器镜像服务中创建）
NAMESPACE=mycompany

# 镜像版本标签
VERSION=latest


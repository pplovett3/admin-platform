# 临时服务器（172.17.80.26）— 接入与部署参考手册

> 用途：原服务器 `172.17.136.200` 临时宕机，启用这台 **Windows Server 2022** 作为临时承载机，供各子系统紧急部署、明天演示。
> 本文档给各子系统负责人参考：如何连接、如何把静态/前端站点部署上去、端口规划、以及门户跳转地址。
> 创建时间：2026-07-28　有效期：演示期间（原服务器恢复后迁回）

---

## 一、服务器基本信息

| 项目 | 值 |
|---|---|
| IP（内网静态） | `172.17.80.26` |
| 操作系统 | Windows Server 2022 Standard（21H2, build 20348.169） |
| 主机名 | `WIN-RACIRV0PP7C` |
| CPU | Intel Xeon Platinum 8582C ×2 |
| 内存 | 128 GB |
| 已装 Web 服务 | **IIS 10**（Web-Server 角色，含管理工具） |
| 远程接入 | RDP 3389（已开）/ OpenSSH 22（已开） |
| 管理员账户 | `administrator`　密码：`enine@198` |

> 内网服务器，仅在校园网 `172.17.x.x` 段可访问。

---

## 二、如何连接

### 2.1 RDP（图形界面，最直观）

- 远程桌面连接 → 计算机：`172.17.80.26` → 用户名 `administrator` / 密码 `enine@198`。

### 2.2 SSH（命令行 / 自动化部署）

已启用 Windows OpenSSH 服务端，默认 shell 为 PowerShell。

```bash
ssh administrator@172.17.80.26
# 密码: enine@198
```

仓库 `.tools/` 下有两个 paramiko 脚本，连接信息可用环境变量或参数传入：

| 脚本 | 作用 | 用法 |
|---|---|---|
| `ssh_run_win.py` | 在服务器执行一条 PowerShell 命令 | `python ssh_run_win.py "<命令>" --user administrator` |
| `sftp_upload_win.py` | 上传文件/目录到服务器 | `python sftp_upload_win.py <本地路径> <远程目录> --user administrator` |

密码通过环境变量 `WIN_PASS` 或 `--pass` 传入，避免明文留在命令行：

```powershell
$env:WIN_PASS="enine@198"
python .tools\ssh_run_win.py "Get-Website" --user administrator
```

> ⚠️ 演示结束后建议改密码并关闭 SSH 密码登录。

---

## 三、已部署服务与端口规划

### 3.1 已占用端口

| 端口 | 服务 | 说明 |
|---|---|---|
| 22 | OpenSSH | 远程命令 |
| 3389 | RDP | 远程桌面 |
| 80 | IIS 默认站点 | 默认 Web Site（空，可不用） |
| **8070** | **总门户** | 全生命周期智慧工地虚拟仿真平台（已部署） |
| **3001** | **三维课件编辑器（admin-platform）** | Next.js 前端 3001 + Node 后端 127.0.0.1:4000 + MongoDB 127.0.0.1:27017（已部署，`C:\admin-platform`，计划任务开机自启） |

### 3.2 各子系统目标端口（与原服务器保持一致，门户跳转已指向本机）

| 子系统 | 端口 | 门户跳转地址 |
|---|---|---|
| 施工管理模块 | 8080 | `http://172.17.80.26:8080` |
| 运管监测模块 | 8088 | `http://172.17.80.26:8088` |
| 运管机器人模块 | 8084 | `http://172.17.80.26:8084` |
| 运管预报预警模块 | 8085 | `http://172.17.80.26:8085` |
| 泵站运行工虚拟仿真实训软件 | 8086 | `http://172.17.80.26:8086` |
| 智能泵站全工况科研虚拟仿真平台 | 8083 | `http://172.17.80.26:8083` |
| 泵站渗流与稳定性综合计算系统 | 8081 | `http://172.17.80.26:8081` |

> 各子系统部署到本机后，**端口保持上表不变**，门户无需改动即可跳转。

### 3.3 总门户地址

- `http://172.17.80.26:8070`（已上线，IIS 站点名 `Portal`，物理路径 `C:\inetpub\portal`）

---

## 四、部署静态/前端站点（IIS，推荐）

适合：纯静态站、Vite/React 打包后的 `dist/`、Vue/Angular 构建产物。

### 4.1 上传文件

把构建产物（含 `index.html`）上传到服务器某目录，例如 `C:\inetpub\<子系统>`：

```powershell
$env:WIN_PASS="enine@198"
python .tools\sftp_upload_win.py "D:\你的项目\dist" "C:\inetpub\yourmod" --user administrator
```

### 4.2 建站点并绑端口（在服务器 PowerShell 执行）

```powershell
Import-Module WebAdministration
New-Website -Name "YourMod" -PhysicalPath "C:\inetpub\yourmod" -Port 8086 -Force
Start-Website -Name "YourMod"
# 防火墙放行
New-NetFirewallRule -Name "YourMod-8086" -DisplayName "YourMod (8086)" -Direction Inbound -Protocol TCP -LocalPort 8086 -Action Allow
```

### 4.3 SPA 路由回退（history 模式）

若前端用 history 路由，刷新子路径会 404。在站点根目录放一个 `web.config`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="SPA Fallback" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
```

> 需在服务器装 URL Rewrite 模块（`Microsoft Web Platform Installer` 装 "URL Rewrite"）。若没装且不便装，前端改用 hash 路由可免此配置。

### 4.4 默认文档

确保 `index.html` 在默认文档里（IIS 默认已含）。

### 4.5 验证

```powershell
Invoke-WebRequest "http://127.0.0.1:8086" -UseBasicParsing
```

外部访问 `http://172.17.80.26:8086`。

---

## 五、需要跨域隔离（COOP/COEP）的站点

如 `pump-station-sim`（高斯泼溅需 `SharedArrayBuffer`），IIS 需下发响应头。在站点 `web.config` 加：

```xml
<system.webServer>
  <httpProtocol>
    <customHeaders>
      <add name="Cross-Origin-Opener-Policy" value="same-origin" />
      <add name="Cross-Origin-Embedder-Policy" value="require-corp" />
    </customHeaders>
  </httpProtocol>
</system.webServer>
```

---

## 六、部署后端服务（非静态）

IIS 只适合静态/反代。若子系统是 Node/Python 后端服务：

1. 把服务跑成后台进程或用 `nssm`/计划任务做成 Windows 服务，监听某端口（如 4000）。
2. IIS 可作为反代（需 Application Request Routing + URL Rewrite），或直接让用户访问 `http://172.17.80.26:4000`，并开防火墙：
   ```powershell
   New-NetFirewallRule -Name "App-4000" -DisplayName "App (4000)" -Direction Inbound -Protocol TCP -LocalPort 4000 -Action Allow
   ```

> 演示场景建议尽量用静态构建 + IIS，最少依赖、最快上线。

---

## 七、运维常用命令（PowerShell）

```powershell
Import-Module WebAdministration
Get-Website                                 # 列站点
Get-WebBinding                              # 列绑定
Start-Website -Name "Portal"                # 启动
Stop-Website -Name "Portal"                 # 停止
Remove-Website -Name "YourMod"               # 删除站点
Get-NetTCPConnection -State Listen | Select LocalPort -Unique   # 监听端口
Get-NetFirewallRule | Where DisplayName -like "*8086*"           # 查防火墙规则
iisreset                                   # 重启 IIS
```

---

## 八、故障排查

| 现象 | 排查 |
|---|---|
| 外部访问超时 | 防火墙未放行端口 → `New-NetFirewallRule` |
| 403 Forbidden | 默认文档无 index.html，或目录无读取权限 |
| 刷新子路径 404 | SPA 未配 web.config 回退规则（见 4.3） |
| 中文乱码 | 确保文件 UTF-8 保存；IIS 已默认 UTF-8 |
| 端口被占 | `Get-NetTCPConnection -LocalPort <端口>` 查占用 |

---

## 九、回迁原服务器

原服务器 `172.17.136.200`（Ubuntu + Docker）恢复后：

1. 各子系统按原方式（docker-compose）迁回原端口。
2. 门户改回 `172.17.136.200` 版本（`portal/index.html`，IP 未改，可直接用）。
3. 本机可下线或保留作备用。

---

## 附录：本次门户部署记录（供复现）

```
# 1. 开 SSH（已在服务器 RDP 内执行）
Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0
Start-Service sshd; Set-Service sshd -StartupType Automatic
New-ItemProperty -Path "HKLM:\SOFTWARE\OpenSSH" -Name DefaultShell -Value "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe" -PropertyType String -Force
New-NetFirewallRule -Name "OpenSSH-22" -Direction Inbound -Protocol TCP -LocalPort 22 -Action Allow
New-NetFirewallRule -Name "Portal-8070" -Direction Inbound -Protocol TCP -LocalPort 8070 -Action Allow

# 2. 装 IIS
Install-WindowsFeature -Name Web-Server -IncludeManagementTools

# 3. 上传门户到 C:\inetpub\portal（含 index.html + assets\school-logo.png）

# 4. 建站点
Import-Module WebAdministration
New-Website -Name "Portal" -PhysicalPath "C:\inetpub\portal" -Port 8070 -Force
Start-Website -Name "Portal"

# 5. 验证 http://172.17.80.26:8070  → 200
```

门户源文件（已改 IP 为 172.17.80.26）：`portal-deploy-8026/`。
原版（IP 172.17.136.200）：`portal/`。
